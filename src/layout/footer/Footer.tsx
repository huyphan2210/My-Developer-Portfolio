import { FC } from "react";
import "./Footer.scss";
import SocialLinks from "../../components/SocialLinks/SocialLinks";

const Footer: FC = () => {
  return (
    <footer>
      <SocialLinks />
    </footer>
  );
};

export default Footer;
